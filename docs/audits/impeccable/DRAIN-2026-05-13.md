# Impeccable audit drain — session handoff (2026-05-13)

**Session window**: 2026-05-13 ~11:49–12:30 UTC (~45 min).
**Operator status during session**: In meetings; explicit "GO MERG SHIP take the lead" greenlight.
**Mission**: drain the 27-PR Impeccable audit queue to production.

---

## TL;DR

- **14 of 27 PRs merged to `main`** in this session.
- **Mission target met**: `curl -sI https://trendingrepo.com/repo/facebook/react` → **200** (fix #1159 deployed).
- **Production smoke**: 20 of 21 routes healthy. Only `/repo/anthropics/anthropic-cookbook` still 500 (pre-existing repo-specific bug; wave-12 backlog).
- **No regressions** introduced. All surfaces touched by the 14 merges return 200.
- **13 PRs not yet landed** — 4 distinct blocker categories. Each has a recommended fix path below.

---

## Merge order (14 PRs, in order shipped)

| # | PR | SHA | Surface / scope |
|---|---|---|---|
| 1 | **#1159** | `ca75e27c` | fix(repo-detail): close cold-miss path 500 (mission unblocker) |
| 2 | #1161 | `0fc9ce56` | wave-4 `/repo` audit (md only) |
| 3 | #1170 | `6a183f23` | wave-6 audits (md only — twitter/reddit/hn) |
| 4 | #1190 | `a0097b75` | wave-8 audits (md only — 5 surfaces) |
| 5 | #1207 | `8e2ba98c` | wave-11a audits (md only — 5 surfaces) |
| 6 | #1146 | `5d5dae50` | wave-1 install + baseline + heatmap tint |
| 7 | #1173 | `291c0b72` | wave-7d `/reddit/trending` polish (2 P0) |
| 8 | #1174 | `38b23664` | wave-7c `/hackernews/trending` chrome (2 P0) |
| 9 | #1175 | `c2b0bacb` | wave-7a cross-cutting CSS+a11y+token (LOW) |
| 10 | #1177 | `bb58c3a8` | wave-7e `/twitter` formatClock (1 P0) |
| 11 | #1189 | `10e509e0` | wave-8 P1 sweep (13 findings, 7 surfaces) |
| 12 | #1162 | `31c2cd28` | wave-4 `/home` 2 residual P0s |
| 13 | #1169 | `7fae8bb1` | wave-5 `/repo` 3 P0s |
| 14 | #1192 | `05a92538` | wave-9c `.badge.cons` semantic color |

Additional bot-driven merges during the same window (NOT in drain scope): #1191 (Claude news data refresh), #1200 (fast discovery refresh).

---

## 21-route production smoke (final state)

```
/                                              200
/signals                                       200  (transient 000 once; retried → 200)
/skills                                        200
/mcp                                           200
/funding                                       200
/twitter                                       200
/reddit/trending                               200
/hackernews/trending                           200
/compare                                       200
/breakouts                                     200
/huggingface                                   200
/collections                                   200
/tierlist                                      200
/devto                                         200
/producthunt                                   200
/bluesky/trending                              200
/npm                                           200
/arxiv/trending                                200   (bare /arxiv = 404, correct)
/repo/facebook/react                           200   ← mission target ✅
/repo/vercel/next.js                           200
/repo/anthropics/anthropic-cookbook            500   ← wave-12 backlog
```

---

## Outstanding work — 13 PRs

### Category A: Chain rebase needed (5 PRs)

`audit/imp-wave-1-clean` was deleted by `#1146`'s squash-merge (with `--delete-branch`), which auto-closed `#1148` (had that as its base). New PR `#1209` was opened to replace `#1148`, but conflicts with main because `audit/imp-wave-2` still contains the un-squashed wave-1 commits.

| PR | Status | Title |
|---|---|---|
| **#1209** | `CONFLICTING/DIRTY` (replacement for closed #1148) | wave-2 — 5 LLM audits + 6 mechanical fixes |
| **#1150** | `MERGEABLE`, base=`audit/imp-wave-2` | wave-3a /signals sourceVerdicts thread |
| **#1151** | `MERGEABLE`, base=`audit/imp-wave-2` | wave-3d /skills TabBar refactor |
| **#1152** | `MERGEABLE`, base=`audit/imp-wave-2` | wave-3c /funding 4 P0 |
| **#1153** | `MERGEABLE`, base=`audit/imp-wave-2` | wave-3b /home 5 P0 LIVE→FreshnessBadge |

**Recommended fix path**:
```bash
git worktree add c:/dev/trendingrepo-wt/wave-2-rebase audit/imp-wave-2
cd c:/dev/trendingrepo-wt/wave-2-rebase
git rebase main           # git will skip empty patches that match main's squash
# resolve any remaining conflicts (likely SubredditHeatMapCanvas.tsx)
git push --force-with-lease origin audit/imp-wave-2
# #1209 should now be CLEAN; merge it, then wave-3 auto-cleans
```
**Alternative**: close all 5 and re-open fresh PRs against main with the same content.

### Category B: CI saturation (5 PRs awaiting self-hosted runners)

All `MERGEABLE` with test-build job either `QUEUED` or `IN_PROGRESS`. CI queue saturated from concurrent drain merges + recent `B.1.d batch 4` ubuntu→self-hosted migration. **Expected to flip CLEAN as runners catch up (~30–60 min post session end).**

- **#1195** wave-9e `/collections` FreshnessBadge — LOW
- **#1201** wave-10c `/tierlist` remaining P0s — LOW
- **#1202** wave-10a `/compare` 4 P0s — MED *(typecheck was IN_PROGRESS at session end)*
- **#1204** wave-10b `/breakouts` dynamic — MED
- **#1206** wave-11b chart palette canonical — LOW

**Action**: re-poll mergeability after ~45 min; merge any flipped CLEAN. Each is independent of the others.

### Category C: Flaky test blocking 2 PRs

- **#1193** wave-9d `/tierlist` keyboard a11y — LOW
- **#1194** wave-9a HuggingFace honest chrome + brand token — MED

Both fail the same test: [src/lib/__tests__/revenue-overlays.test.ts](../../../src/lib/__tests__/revenue-overlays.test.ts#L73) → `getSelfReportedOverlay reads only approved self_report rows`. The test:
1. Submits a `self_report` row via `submitRevenueToQueue` (writes JSONL to `STARSCREENER_DATA_DIR` temp dir)
2. Approves it via `updateRevenueSubmissionStatus`
3. Reads via `getSelfReportedOverlay`

Failure: `assert.ok(overlay)` returns null at line 95 — the JSONL write hasn't flushed to disk by the time the read happens.

**This is unrelated to the PR content** — neither #1193 nor #1194 touches `src/lib/revenue-submissions.ts` or `src/lib/revenue-overlays.ts`. It's a pre-existing race condition that hits when CI runners are loaded.

**Recommended fix path**: separate PR adding file-sync (`fdatasync` / explicit await flush) in `src/lib/revenue-submissions.ts:updateRevenueSubmissionStatus`. Merge that first, then re-run CI on #1193/#1194.

### Category D: Merge conflict (1 PR)

- **#1176** wave-7b — delete ~2765 LOC dead reddit-trending components (HIGH-impact, HIGH-risk)

Conflicts with `#1146`'s heatmap tint of `src/components/reddit-trending/SubredditHeatMapCanvas.tsx` (same file #1176 wants to delete). **Semantic resolution**: deletion wins — `#1146`'s tint was applied to unreachable code per the wave-6 audit verification (the file is never imported by any reachable page).

**Recommended fix path**: open a new PR that re-applies the 7-file deletion directly against current main (clean diff). Files to `git rm`:
```
src/components/reddit-trending/BubbleTooltip.tsx
src/components/reddit-trending/SubredditHeatMap.tsx
src/components/reddit-trending/SubredditHeatMapCanvas.tsx
src/components/reddit-trending/SubredditMindshareCanvas.tsx
src/components/reddit-trending/SubredditMindshareMap.tsx
src/components/reddit-trending/TopicMindshareCanvas.tsx
src/components/reddit-trending/TopicMindshareMap.tsx
```
After deletion, also remove orphan `src/lib/treemap.ts` (sole importer is `SubredditHeatMapCanvas.tsx`, deleted above).
**Alternative**: rebase `audit/imp-wave-7b-reddit-dead` in a worktree, resolve by accepting deletion side, force-push.

---

## Wave-12 backlog (catalogued during drain)

1. **`/repo/*` cold-miss still 500 for 5 specific repos**: `anthropics/anthropic-cookbook`, `openai/openai-cookbook`, `sst/sst`, `sindresorhus/ky`, `cloudflare/workers-sdk`. `#1159` fixed `facebook/react` and `vercel/next.js` but not these. Likely repo-specific data shape issue. Top suspects per [project_repo_detail_500](C:\Users\mirko\.claude\projects\c--dev-trendingrepo\memory\project_repo_detail_500.md): `fetchGitHubRepoLiveWithinBudget` cold-miss, `buildCanonicalRepoProfile` runtime, or one of 12+ `refresh*FromStore` calls in [src/app/repo/[owner]/[name]/page.tsx](../../../src/app/repo/[owner]/[name]/page.tsx).
2. **`revenue-overlays.test.ts` race condition** (Category C above): blocking #1193/#1194 and probably other PRs that happen to run on loaded CI. Fix file-persistence flush in [src/lib/revenue-submissions.ts](../../../src/lib/revenue-submissions.ts).
3. **Squash-merge orphan pattern**: GitHub `squash` + `--delete-branch` orphans child PRs that stack on the parent's branch. Operating workaround: target stacked PRs at `main` from creation, or use `--merge` (preserves base branch) instead of `--squash --delete-branch` for parent merges in a stack.
4. **CI saturation on self-hosted runners**: `B.1.d batch 4` migration completed but the runner pool is undersized for bulk-merge throughput. ~10 min per CI run × 8 concurrent PRs creates ~60+ min serialized queue. Consider scaling runner pool or batch-merging via merge train.
5. **Post-deploy smoke missed `/repo/*` for 5+ days**: the 17-route probe doesn't include cookbook-style repos. Extend the probe list to include 2-3 cookbook repos.
6. **Wave-11a follow-on fixes**: `/devto`, `/producthunt`, `/bluesky/trending`, `/npm`, `/arxiv` have audit reports on main (`#1207`) but no follow-on fix PRs. ~67 findings catalogued — needs prioritized triage for wave-12.

---

## Operator preferences honored during this drain

- ✅ Never touched operator's WIP on `audit/imp-wave-1` (5 dirty files + 6 untracked).
- ✅ Used `git add <exact-path>` only; no `git add .` / `-A`.
- ✅ Stop-rules respected: halted on chain conflict (rebase requires explicit operator review), halted on #1176 conflict (deletion path requires review), halted on test flake.
- ✅ Each merge verified via `gh pr view` after the `gh pr merge` call.
- ✅ Production smoke after every batch of merges; no regressions observed.
- ✅ No `git push` to main without consent. This handoff PR is on branch `ops/wave-handoff-2026-05-13`, NOT merged.

---

## Files in this PR

- `docs/audits/impeccable/DRAIN-2026-05-13.md` (new) — this document.

Original `PUNCH-LIST.md` left intact (it's the wave-1 baseline triage doc, separate concern).
