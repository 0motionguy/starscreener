---
last-verified: 2026-05-05
verified-by: claude
status: living
---

# Next Wave — Forward Plan (2026-05-06+)

The Phase 1 docs restructure (silly-frost) is closed. This doc is the
ordered backlog for the NEXT wave of structural work, broken into
independent tasks suitable for parallel sub-agent dispatch.

Each task lists: scope, dependencies, sub-agent prompt seed, verification.

---

## Wave A — Doc rewrites (drift-driven)

**Why first:** the surviving 10 snapshot docs are the next-most-likely
sources of agent-context poisoning. They claim to be authoritative but
aren't.

### A1. ROOT — DEVELOPMENT.md (currently `needs-verification`)
- **Scope:** drift-audit local-dev instructions against current `package.json` scripts + Node 22 reality.
- **Verify:** every `npm run X` referenced exists.
- **End state:** `status: living` with refreshed dev-loop section.

### A2. ROOT — SPRINTS.md (currently `needs-verification`)
- **Scope:** drift-audit. Sprint plans go stale fast.
- **Decision:** if it's actively used, refresh; if abandoned, demote to `archive`.

### A3. docs/forensic/00-INDEX.md (regenerating since gitignore landed)
- **Scope:** investigate why the forensic INDEX keeps re-appearing in `docs/forensic/` despite `.gitignore`. Patch the regenerator OR delete-and-prevent.

### A4. tasks/CURRENT-SPRINT.md + tasks/BACKLOG.md (currently `needs-verification`)
- **Scope:** per-line drift check vs. live Paperclip board.
- **Dependency:** Paperclip API up at `http://localhost:3030` (or override via `PAPERCLIP_BASE_URL`).
- **Run:** `node scripts/reconcile-tasks-with-paperclip.mjs` (already wired).
- **End state:** closed/open/missing tables in `tasks/reconciliation-2026-05-06.md`; sprint+backlog promoted to `living` after closures applied.

### A5. The 6 remaining unlabeled docs
- **Scope:** find them via `find docs/ tasks/ -name "*.md" -exec head -1 {} \; | grep -v "^---"` (rough heuristic).
- **End state:** every doc in scan-scope has frontmatter.

---

## Wave B — Hooks + tooling polish

### B1. Verify hooks fire end-to-end in the actual harness
- **Why:** Agent 7 found `post-edit.mjs` was wired with a bogus `$CLAUDE_TOOL_INPUT_FILE_PATH` env var. Fix landed in Wave 2 (stdin JSON path), but no real-world fire confirmation yet.
- **Method:** edit a TS file in a fresh Claude Code session; observe whether the harness invokes the hook and surfaces tsc errors.
- **End state:** documented stdin-event format in `.claude/hooks/README.md`.

### B2. Hook bypass UX
- **Why:** Agent G + Agent 10 had to use Bash heredoc to bypass `protect-files.mjs` for legitimate workflow edits.
- **Decision:** either widen the protected matcher to also cover Bash writes (defense in depth) OR add a `BYPASS_PROTECTION=true` env-var override with a one-liner explainer in `.claude/hooks/protect-files.mjs`.

### B3. Pre-commit lint-staged config
- **Why:** commit `04f2bdf5` notes "pre-commit lint-staged config missing".
- **Scope:** add `.husky/pre-commit` + `package.json` lint-staged block. Wire `npm run lint`, `npm run typecheck`, the 4 guard scripts.

### B4. Engine-inventory CI watchdog
- **Why:** `.github/workflows/engine-inventory-refresh.yml` runs weekly; nothing catches a workflow added mid-week with the wrong cron.
- **Scope:** add a PR-gate step that fails if `docs/_generated/engine.json` is out of date with `.github/workflows/`.

---

## Wave C — Architecture follow-throughs

### C1. ADR 0001 follow-up — pick a permanent direction
- **Decision needed:** wire phase 1a (~1d engineering) OR draft ADR 0004 superseding 0001 with "Redis primary + worker-only Supabase analytics" as the permanent design.
- **Owner:** Basil + claude.

### C2. Worker fetcher count truth
- **Why:** registry says 44 active, filesystem has 51, 7 are stubs.
- **Scope:** delete the 7 stubs OR add them to the registry. Eliminate the count-drift root cause.

### C3. Workflow count growth gate
- **Why:** workflow count grew 62 → 85 between audit cycles.
- **Scope:** add a `.github/CODEOWNERS`-style approval gate: any `.github/workflows/*.yml` add requires an entry in `docs/ENGINE.md` in the same PR.

### C4. CLAUDE.md content audit
- **Why:** Agents A/D/L found drift in the project's own CLAUDE.md across the day (worker count, Twitter provider, ENGINE freshness tag).
- **Scope:** end-to-end re-read of `CLAUDE.md` with grep verification of every numbered claim. Stamp `last-verified`.

---

## Wave D — Docs that need rewriting (BACKLOG-tracked)

### D1. docs/DESIGN_SYSTEM.md still flagged needs-rewrite
- Already redirected to `design/v4/DESIGN_SYSTEM.md` via pointer. Verify the pointer works; consider deleting the old file entirely.

### D2. docs/REDIS-TTL-CONVENTIONS.md uses prose `Status:` instead of YAML frontmatter
- One-line fix: convert the prose status to YAML frontmatter so `check-living-docs-have-frontmatter` recognizes it.

### D3. docs/_generated/hook-smoke-test.md keeps regenerating unlabeled
- Fix the smoke-test generator to write frontmatter on output.

---

## Wave E — Long-tail cleanup

### E1. AGENTS.md fetcher count drift (Agent I flagged)
- AGENTS.md line 13 still says "52 fetchers" — should be 51.

### E2. OPERATOR.md fetcher count drift (Agent I flagged)
- OPERATOR.md line 43 (ASCII box) still says "52 fetchers cron-fired" — should be 51.

### E3. Worklog hygiene script (created Wave 3)
- `scripts/archive-old-worklogs.mjs` exists but never auto-runs. Wire into a weekly GH Action.

### E4. Doc-link broken targets in `docs/review/*`
- All 4 remaining broken links live in archived review docs. Decision: either fix the links inside the archived docs (preserves archaeology) OR teach `check-internal-doc-links.mjs` to skip files with `status: archive` frontmatter.

### E5. `docs/SESSION_HANDOFF.md` and `docs/NEXT_SESSION.md`
- Per Phase 1.0.D Agent classification: candidates for `docs/archive/handoffs/`. Move + redirect.

### E6. Memory note links from RUNBOOK-secret-rotation.md
- Phase 1.1 fixed inbound refs but the doc still references `~/.claude/projects/.../memory/...` paths that may not resolve cross-platform.

---

## Wave F — Higher-order structural

### F1. `src/features/` migration (deferred from silly-frost plan)
- **Status:** deferred. The current layer-dominant layout has 55 already-domain-themed component subfolders. Re-evaluate only if a specific feature shows scatter pain.

### F2. Redis namespace migration (`pool:`, `ratelimit:`, `recovery:` → `ss:pool:`, etc.)
- **Why:** Phase 4 preserved existing namespaces verbatim. A future migration could unify them under the `ss:` prefix.
- **Effort:** dual-read/dual-write Redis migration; non-trivial.
- **Trigger:** when we rebuild Redis or do a major version bump.

### F3. Auto-generated docs for OpenAPI surface
- **Why:** `docs/openapi.yaml` is currently hand-maintained.
- **Scope:** generate from Zod schemas at the route boundaries (already validated per CLAUDE.md). Single source of truth.

---

## Execution model

**For each wave:**
1. Read this file + `tasks/BACKLOG.md`.
2. Pick a wave letter (A/B/C/D/E/F).
3. Dispatch 1 sub-agent per item OR batch 3-5 items per agent if scope is small.
4. Run all 4 guard scripts after the wave.
5. Commit with subject prefix `docs(restructure):`, `chore(wave-X):`, etc.

**Verification gate:** every commit on this branch line should have ALL of:
- `node scripts/check-docs-freshness.mjs` — exit 0
- `node scripts/check-living-docs-have-frontmatter.mjs` — exit 0
- `node scripts/check-redis-keys.mjs` — exit 0
- `npm run typecheck` — no NEW errors (pre-existing OK)

**Smallest-ROI-quickest-wins (do these first):**
- D2 (REDIS-TTL frontmatter): 1 edit, 5 min
- E1 + E2 (fetcher count drift): 2 edits, 5 min
- D3 (hook-smoke-test generator): 1 edit, 10 min
- A5 (6 unlabeled docs): 6 edits, 15 min

**Big architecture calls (need Basil):**
- C1 (ADR 0001 wire-or-supersede)
- C2 (worker fetcher stubs delete-or-register)
- F2 (Redis namespace unification)

---

## Reference

- This file: `tasks/NEXT-WAVE-2026-05-06.md`
- Today's commit chain: `8b845df6`, `e4737757`, `4ae6b74f`, `48a5e1c3`
- Front door: `docs/INDEX.md`
- Plan that closed today: `~/.claude/plans/structuring-a-repository-silly-frost.md`
